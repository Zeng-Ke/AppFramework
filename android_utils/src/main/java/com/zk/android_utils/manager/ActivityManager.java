package com.zk.android_utils.manager;

import android.app.Activity;

import java.util.ArrayList;
import java.util.List;

/**
 * author: ZK.
 * date:   On 2018/6/7.
 */
public class ActivityManager {


    /*private static List<Activity> mActivityList = new ArrayList<>();


    public static void add(Activity activity) {
        mActivityList.add(activity);
    }


    public static void removeLastThis(Activity activity) {
        int length = mActivityList.size();
        if (length <= 0)
            return;
        for (int i = length - 1; i < length; i--) {
            if (activity.getClass().equals(mActivityList.get(i).getClass())) {
                mActivityList.remove(i);
                break;
            }
        }
    }


    public static void removeAllThis(Class activityClz) {
        int length = mActivityList.size();
        if (length <= 0)
            return;
        for (int i = length - 1; i < length; i--) {
            if (activityClz.equals(mActivityList.get(i).getClass()))
                mActivityList.remove(i);
        }
    }

    public static void removeLast() {
        int length = mActivityList.size();
        if (length <= 0)
            return;
        mActivityList.remove(length - 1);
    }

    public  static  void  removeAllButFirst(){



    }*/


}
